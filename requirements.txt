# WhatsApp IP Leak Detection - Requirements
# Install with: pip3 install -r requirements.txt

# Core networking and packet manipulation
scapy>=2.4.5

# HTTP requests for external API calls
requests>=2.25.1

# Enhanced output formatting (optional)
colorama>=0.4.4
termcolor>=1.1.0

# Better logging (optional)
python-dateutil>=2.8.2

# Network interface detection (optional)
netifaces>=0.11.0

# Enhanced WHOIS parsing (optional)
python-whois>=0.7.1

# GeoIP lookups (alternative to IPGeoLocation)
geoip2>=4.1.0

# Real-time packet analysis (optional)
pyshark>=0.6.0

# Configuration management (optional)
pyyaml>=5.4.1

# Progress bars (optional)
tqdm>=4.62.0